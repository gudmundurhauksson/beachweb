import { Match } from "./match";
import { DivisionMatch } from "./divisionMatch";

export class GroupView {
    matches: Array<DivisionMatch>;
    division: number;
    group: number;
    type: number;
}